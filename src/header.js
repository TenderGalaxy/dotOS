// A header added before worldcode in TEST_unified for preliminary bug-fixing
export default {
    func(){
        myId = -1
        api = {setCameraDirection(){},getPosition(){},getPlayerFacingInfo(){return {dir: []}}}
    }
}